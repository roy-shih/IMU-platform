const config = {
  version: "v1",
  author: ["ZoneTwelve"], // just a random staff
  maintainer: ["ZoneTwelve"], // another random staff
  route: "route",
  socket: "socket",
}

module.exports = config;